# jfox show --json 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `jfox show` 加 `--json` / `--format json` 输出，返回结构化 JSON（默认 raw Markdown 行为不变）。

**Architecture:** 复用 git stash commit `d2df418` 的 WIP 设计。`models.py` 加 `Note.to_show_dict()` 序列化方法；`cli.py` 的 `_show_impl` 加 `output_format` 参数、`show` 命令加 `--format`/`--json` 双入口，错误按模式分流。复用现有 `output_json()`。

**Tech Stack:** Python 3.10+, Typer, dataclasses。测试用 pytest + unittest.mock + Typer CliRunner。

## Global Constraints

- **分支**：`feat/show-json-278`（已建，base = `e930ffe`）。`main` 受保护，禁直接 commit。
- **Stage 纪律**：`git add <具体文件>`，禁 `git add -A`（工作区有无关 untracked 文件）。
- **commit 前**：`git branch --show-current` 确认在 `feat/show-json-278`（本机有并发 git 活动）。
- **测试运行**：`uv run pytest tests/unit/test_show.py`（纯 mock 单测，不涉 embedding/ChromaDB，可自主跑；`pytest.ini` 已含 `-v`，勿重复加）。
- **行宽**：100 字符（black + ruff）。
- **注释/文档**：中文。
- **commit message 结尾**：`Co-Authored-By: Claude <noreply@anthropic.com>`。

---

## File Structure

| 文件 | 职责 | 改动 |
|------|------|------|
| `jfox/models.py` | `Note` 数据模型。新增 `to_show_dict()` 方法（与 `to_dict`/`to_markdown` 同级）。 | 在 `to_dict` 方法后（约 line 250）插入新方法 |
| `jfox/cli.py` | CLI。`_show_impl` 加 `output_format` 参数；`show` 命令加 `--format`/`--json` + 错误分流。 | 改 `line 1176` `_show_impl` + `line 1194` `show` |
| `tests/unit/test_show.py` | show 命令单测。修现有断言 + 加 JSON 测试 + 加 `to_show_dict` 真实逻辑测试。 | 新增 ~9 个测试，修 1 个断言 |

**接口契约：**
- Task 1 产出 `Note.to_show_dict(raw_markdown: Optional[str] = None) -> Dict[str, Any]`（Task 2 消费）。
- Task 2 改 `_show_impl(note_ref: str, output_format: str = "markdown")`，json 分支调 `n.to_show_dict(raw_markdown=raw)`。

---

## Task 1: `Note.to_show_dict()` 序列化方法

**Files:**
- Modify: `jfox/models.py`（在 `to_dict` 方法 `return d` 之后插入，约 line 250）
- Test: `tests/unit/test_show.py`

**Interfaces:**
- Consumes: `Note` 字段（id/title/type/created/updated/tags/links/backlinks/topic/source/archived + candidate 专属）、`re`（已 import）、`NoteType.CANDIDATE`、`GemLevel.FLAWED`、`filepath` property。
- Produces: `Note.to_show_dict(raw_markdown=None) -> Dict[str, Any]`，字段集见 Step 3。

- [ ] **Step 1: 写失败测试 — 基础字段 + content_body 剥 frontmatter**

在 `tests/unit/test_show.py` 末尾追加（文件顶部已有 `from unittest.mock import ...` 等导入）：

```python
    def test_to_show_dict_basic_fields(self):
        """测试 to_show_dict 基础字段 + content_body 剥 frontmatter"""
        from datetime import datetime

        from jfox.models import Note, NoteType

        raw = (
            "---\nid: '202604141200001234'\ntitle: 测试\n"
            "type: permanent\ncreated: '2026-04-14T12:00:00'\n"
            "updated: '2026-04-14T12:00:00'\ntags: []\n---\n\n# 测试\n\n正文内容"
        )
        from pathlib import Path

        n = Note(
            id="202604141200001234",
            title="测试",
            content="# 测试\n\n正文内容",
            type=NoteType.PERMANENT,
            created=datetime(2026, 4, 14, 12, 0, 0),
            updated=datetime(2026, 4, 14, 12, 0, 0),
        )
        n.set_filepath(Path("/tmp/202604141200001234.md"))  # 固定路径，避免 config 依赖
        d = n.to_show_dict(raw_markdown=raw)
        assert d["id"] == "202604141200001234"
        assert d["title"] == "测试"
        assert d["type"] == "permanent"
        assert d["content"] == raw
        assert d["content_body"] == "# 测试\n\n正文内容"
        assert d["filepath"] == str(Path("/tmp/202604141200001234.md"))
        # source 为 None / archived 为 False 时不输出
        assert "source" not in d
        assert "archived" not in d

    def test_to_show_dict_candidate_fields(self):
        """测试 candidate 笔记含专属字段"""
        from datetime import datetime

        from jfox.models import GemLevel, Note, NoteType

        from pathlib import Path

        n = Note(
            id="202604141200001234",
            title="候选笔记",
            content="内容",
            type=NoteType.CANDIDATE,
            created=datetime(2026, 4, 14, 12, 0, 0),
            updated=datetime(2026, 4, 14, 12, 0, 0),
            confidence=0.8,
            knowledge_type="factual",
            status="pending",
        )
        n.set_filepath(Path("/tmp/candidate.md"))  # 固定路径，避免 config 依赖
        d = n.to_show_dict()
        assert d["gem_level"] == GemLevel.FLAWED.value  # 默认 FLAWED
        assert d["confidence"] == 0.8
        assert d["knowledge_type"] == "factual"
        assert d["status"] == "pending"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/unit/test_show.py::TestShowCommand::test_to_show_dict_basic_fields tests/unit/test_show.py::TestShowCommand::test_to_show_dict_candidate_fields`
Expected: FAIL，报 `AttributeError: 'Note' object has no attribute 'to_show_dict'`

- [ ] **Step 3: 实现 `to_show_dict` 方法**

在 `jfox/models.py` 的 `to_dict` 方法 `return d`（约 line 250）之后、下一个方法/类之前，插入：

```python
    def to_show_dict(self, raw_markdown: Optional[str] = None) -> Dict[str, Any]:
        """转换为 show --json 专用的完整字典。

        Args:
            raw_markdown: 笔记文件的完整原始 Markdown 内容（含 frontmatter）。
                如果未提供，则使用 to_markdown() 重新生成。

        Returns:
            包含完整字段的字典，schema 与 Issue #278 一致。
        """
        content = raw_markdown if raw_markdown is not None else self.to_markdown()
        # content_body: 去掉 frontmatter 后的 Markdown 主体
        body_match = re.match(r"^---\n.*?\n---\n+(.*)", content, re.DOTALL)
        content_body = body_match.group(1) if body_match else content

        d: Dict[str, Any] = {
            "id": self.id,
            "title": self.title,
            "type": self.type.value,
            "created": self.created.isoformat(),
            "updated": self.updated.isoformat(),
            "tags": self.tags,
            "links": self.links,
            "backlinks": self.backlinks,
            "topic": self.topic,
            "filepath": str(self.filepath),
            "content": content,
            "content_body": content_body,
        }
        # 可选附加字段（与 to_markdown 一致）
        if self.source:
            d["source"] = self.source
        if self.archived:
            d["archived"] = self.archived
        # candidate 专属字段
        if self.type == NoteType.CANDIDATE:
            d["gem_level"] = self.gem_level or GemLevel.FLAWED.value
            if self.confidence is not None:
                d["confidence"] = self.confidence
            if self.source_fragments:
                d["source_fragments"] = self.source_fragments
            if self.grounded_by:
                d["grounded_by"] = self.grounded_by
            if self.knowledge_type:
                d["knowledge_type"] = self.knowledge_type
            if self.status:
                d["status"] = self.status
        return d
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/unit/test_show.py::TestShowCommand::test_to_show_dict_basic_fields tests/unit/test_show.py::TestShowCommand::test_to_show_dict_candidate_fields`
Expected: PASS（2 passed）

- [ ] **Step 5: commit**

```bash
git branch --show-current  # 确认 feat/show-json-278
git add jfox/models.py tests/unit/test_show.py
git commit -m "feat(models): Note.to_show_dict for show --json (#278)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: `show` 命令 `--json` / `--format json` 双入口 + 错误分流

**Files:**
- Modify: `jfox/cli.py:1176` `_show_impl`（加 `output_format` 参数 + json 分支）
- Modify: `jfox/cli.py:1194` `show`（加 `--format`/`--json` 参数 + 错误分流）
- Test: `tests/unit/test_show.py`（修断言 + 加 4 个 CLI 测试）

**Interfaces:**
- Consumes: Task 1 的 `Note.to_show_dict(raw_markdown=...)`、现有 `output_json()`（cli.py:124）。
- Produces: `jfox show <ref> [--format json | --json] [--kb X]`，默认 raw Markdown 不变。

- [ ] **Step 1: 修现有断言 + 写失败测试**

1. 修 `tests/unit/test_show.py` 里 `test_show_calls_impl` 的断言（`_show_impl` 增了 `output_format` 参数）。把：

```python
        mock_impl.assert_called_once_with("202604141200001234")
```

改为：

```python
        mock_impl.assert_called_once_with("202604141200001234", "markdown")
```

2. 在文件顶部导入区加 `import json as json_module`（在 `from unittest.mock import ...` 之前一行）。

3. 在 `test_show_impl_not_found` 之后追加 4 个测试：

```python
    @patch("jfox.cli.note.load_note_by_id")
    @patch("jfox.cli.find_note_id_by_title_or_id")
    def test_show_impl_json_output(self, mock_find, mock_load):
        """测试 _show_impl JSON 模式输出结构化 JSON"""
        mock_find.return_value = "202604141200001234"
        raw_md = (
            "---\nid: '202604141200001234'\ntitle: 测试标题\ntype: permanent\n"
            "created: '2026-04-14T12:00:00'\nupdated: '2026-04-14T12:00:00'\n"
            "tags: [t1]\nlinks: []\nbacklinks: []\n---\n\n# 测试标题\n\n正文内容"
        )
        mock_note = MagicMock()
        mock_note.filepath.read_text.return_value = raw_md
        mock_note.to_show_dict.return_value = {
            "id": "202604141200001234",
            "title": "测试标题",
            "type": "permanent",
            "content": raw_md,
            "content_body": "# 测试标题\n\n正文内容",
        }
        mock_load.return_value = mock_note

        import io
        import sys

        from jfox.cli import _show_impl

        captured = io.StringIO()
        sys.stdout = captured
        try:
            _show_impl("测试笔记", output_format="json")
        finally:
            sys.stdout = sys.__stdout__

        data = json_module.loads(captured.getvalue())
        assert data["id"] == "202604141200001234"
        assert data["title"] == "测试标题"
        assert data["content_body"] == "# 测试标题\n\n正文内容"
        mock_note.to_show_dict.assert_called_once_with(raw_markdown=raw_md)

    @patch("jfox.cli._show_impl")
    def test_show_json_flag(self, mock_impl):
        """测试 --json 参数传递给 _show_impl"""
        result = runner.invoke(app, ["show", "202604141200001234", "--json"])
        assert result.exit_code == 0
        mock_impl.assert_called_once_with("202604141200001234", "json")

    @patch("jfox.cli._show_impl")
    def test_show_format_json(self, mock_impl):
        """测试 --format json 参数传递给 _show_impl"""
        result = runner.invoke(app, ["show", "202604141200001234", "--format", "json"])
        assert result.exit_code == 0
        mock_impl.assert_called_once_with("202604141200001234", "json")

    @patch("jfox.cli._show_impl", side_effect=ValueError("笔记不存在: xxx"))
    def test_show_json_error_output(self, mock_impl):
        """测试 JSON 模式下错误返回结构化 JSON"""
        result = runner.invoke(app, ["show", "xxx", "--json"])
        assert result.exit_code != 0
        data = json_module.loads(result.output)
        assert data["success"] is False
        assert "笔记不存在" in data["error"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/unit/test_show.py`
Expected: `test_show_json_flag` / `test_show_format_json` / `test_show_impl_json_output` / `test_show_json_error_output` FAIL（`--json` 选项不存在 / `_show_impl` 不接受 `output_format`）；`test_show_calls_impl` 也 FAIL（断言不匹配新签名）。

- [ ] **Step 3: 改 `_show_impl`（加 output_format + json 分支）**

把 `jfox/cli.py` 现有 `_show_impl`（line 1176）整体替换为：

```python
def _show_impl(note_ref: str, output_format: str = "markdown"):
    """查看笔记完整内容的内部实现"""
    # 通过 ID 或标题定位笔记
    note_id = find_note_id_by_title_or_id(note_ref)
    if not note_id:
        raise ValueError(f"笔记不存在: {note_ref}")

    # 加载笔记
    n = note.load_note_by_id(note_id)
    if not n:
        raise ValueError(f"笔记不存在: {note_id}")

    # 读取原始 Markdown 内容
    raw = n.filepath.read_text(encoding="utf-8")

    if output_format == "json":
        # 输出结构化 JSON，便于脚本和 LLM 消费
        print(output_json(n.to_show_dict(raw_markdown=raw)))
    else:
        # 默认输出原始 Markdown 内容（含 YAML frontmatter）
        print(raw)
```

- [ ] **Step 4: 改 `show` 命令（加参数 + 错误分流）**

把 `jfox/cli.py` 现有 `show` 命令（line 1194）整体替换为：

```python
@app.command()
def show(
    note_ref: str = typer.Argument(..., help="笔记 ID 或标题"),
    output_format: str = typer.Option(
        "markdown", "--format", "-f", help="输出格式: markdown, json"
    ),
    json_output: bool = typer.Option(
        False, "--json", help="JSON 输出（快捷方式，等同于 --format json）"
    ),
    kb: Optional[str] = typer.Option(None, "--kb", "-k", help="目标知识库名称"),
):
    """
    查看笔记完整内容

    默认输出原始 Markdown（含 YAML frontmatter），只读不修改。
    使用 --json 或 --format json 可输出结构化 JSON，便于脚本和 LLM 消费。
    支持通过笔记 ID 或标题定位。
    """
    try:
        # 向后兼容：如果指定了 --json，使用 json 格式
        if json_output:
            output_format = "json"

        from .config import use_kb

        with use_kb(kb):
            _show_impl(note_ref, output_format)

    except Exception as e:
        result = {
            "success": False,
            "error": str(e),
        }
        if output_format == "json":
            print(output_json(result))
        else:
            console.print(f"[red]✗[/red] Error: {e}")
        raise typer.Exit(1)
```

- [ ] **Step 5: 跑全部 show 测试确认通过**

Run: `uv run pytest tests/unit/test_show.py`
Expected: PASS（全部，含原 5 个 + 新增 6 个 = 共 11 个）

- [ ] **Step 6: 手动验证 CLI（端到端冒烟）**

```bash
uv run jfox show --help        # 确认列出 --format / --json
```
Expected: help 文本含 `--format`、`--json`、`--kb`。

（若手头有真实笔记可进一步 `uv run jfox show <id> --json | python -m json.tool` 看输出；无则跳过。）

- [ ] **Step 7: commit**

```bash
git branch --show-current  # 确认 feat/show-json-278
git add jfox/cli.py tests/unit/test_show.py
git commit -m "feat(cli): jfox show 支持 --json 输出 (#278)

--json / --format json 输出结构化 JSON（复用 Note.to_show_dict），
默认 raw Markdown 行为不变；JSON 模式错误返回结构化 {success,error}。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 完成后（实现之外）

- 推送 + 开 PR（github-issue-driven 步骤 8-9，走 zima-pr-monitor）
- PR body 链接 issue #278，`closes #278`
- README 已更新（round 1 CR 要求，命令参考表 L277 加了 --json 说明）。
